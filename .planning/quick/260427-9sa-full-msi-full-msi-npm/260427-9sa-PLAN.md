---
phase: quick
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/lib.rs
autonomous: true
must_haves:
  truths:
    - "Windows full 版本从包内 resources/nodejs 查找 MSI 并安装，不回退到在线下载"
    - "Windows online 版本从互联网下载 MSI 并静默安装"
    - "安装后通过 refresh_windows_path + ensure_windows_node_ready 确保 npm 可用"
  artifacts:
    - path: "src-tauri/src/lib.rs"
      contains: "full 路径走 bundled MSI 发现/安装, online 路径走下载/安装, 统一收尾到 npm readiness"
  key_links:
    - from: "discover_windows_bundled_node_msi"
      to: "install_windows_node_and_verify"
      via: "full 路径: 发现 MSI → 安装 → npm readiness"
    - from: "在线下载"
      to: "install_windows_node_and_verify"
      via: "online 路径: 下载 MSI → 安装 → npm readiness"
---

# Quick Task 260427-9sa Plan

## Goal

审查并清理 `install_node_env` 的 Windows full/online 两条安装链路，确保：
- full 版本只在包内查找 MSI，不 fallback 下载
- online 版本从网络下载 MSI
- 两路径统一以 `install_windows_node_and_verify` 收尾，确保 npm 可用

## Context

- 当前代码已实现 full/online 分歧逻辑
- Linux 旧路径中有死代码（msiexec 调用等）需要清理
- 无需改动功能逻辑，只做清理

## Tasks

### Task 1
- files: `src-tauri/src/lib.rs`
- action: 清理 `install_node_env` 中 Linux 非 Windows bundled 路径的死代码（msiexec 调用、refresh_windows_path 等），这些代码在 `!is_win` 分支中不会执行但增加维护负担。简化 `install_node_env` 结构，让 full/online 两条链路更清晰。
- verify: `cargo test --manifest-path src-tauri/Cargo.toml --lib`
- done: `install_node_env` 结构清晰，full/online 路径分明，Linux 旧代码已清理。
