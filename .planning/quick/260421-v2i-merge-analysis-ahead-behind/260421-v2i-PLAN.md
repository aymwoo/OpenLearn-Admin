---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src-tauri/src/lib.rs]
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "git_status 使用 merge_analysis 计算 ahead/behind"
    - "边界情况正确处理"
  artifacts:
    - path: "src-tauri/src/lib.rs"
      provides: "改进后的 git_status 函数"
      min_lines: 640
  key_links: []
---

<objective>
使用 merge_analysis 修复 ahead/behind 计算不准确问题

Purpose: 当前 git_status 使用 graph_ahead_behind 计算领先/落后提交数，在某些边界情况下可能不准确。使用 merge_analysis 可以获得更可靠的分支关系分析。

Output: 改进后的 git_status 函数
</objective>

<context>
@src-tauri/src/lib.rs

当前 git_status 函数 (lines 558-647):
- 使用 `repo.graph_ahead_behind(local_oid, remote_oid)` 计算 ahead/behind
- merge_analysis 在 fast_forward 函数中已有使用模式 (lines 314-352)
</context>

<tasks>

<task type="auto">
  <name>Task 1: 使用 merge_analysis 改进 ahead/behind 计算</name>
  <files>src-tauri/src/lib.rs</files>
  <action>
修改 git_status 函数，使用 merge_analysis 替代 graph_ahead_behind 来计算 ahead/behind:

1. 获取本地 HEAD 的 Oid
2. 使用 find_remote_commit 获取远端分支的 AnnotatedCommit
3. 调用 repo.merge_analysis(&[&annotated_commit]) 获取分析结果
4. 从 merge_analysis 结果中提取 ahead/behind 信息:
   - 如果 analysis.is_up_to_date() → ahead=0, behind=0
   - 如果 analysis.is_fast_forward() → behind = 远端 commit 数量 - 本地 commit 数量 (需要额外计算)
   - 保留 graph_ahead_behind 作为 fallback

关键: merge_analysis 的 merge_analysis_kind_t 包含了分支关系信息，需要正确解析来获取 ahead/behind。
</action>
  <verify>
  <automated>cargo build --manifest-path src-tauri/Cargo.toml 2>&1 | head -30</automated>
  </verify>
  <done>
git_status 函数返回的 ahead/behind 使用 merge_analysis 计算，边界情况处理正确
</done>
</task>

</tasks>

<success_criteria>
- cargo build 成功
- git_status 返回的 ahead/behind 数值正确
- 边界情况 (如本地为空、远端无引用) 正确处理
</success_criteria>

<output>
After completion, create `.planning/quick/260421-v2i-merge-analysis-ahead-behind/260421-v2i-SUMMARY.md`
</output>
