# Platform Services
Added `start_service` and `stop_service` Tauri commands in `src-tauri/src/lib.rs` and integrated them into the Next.js frontend in `src/app/page.tsx` for cross-platform process management.
