---
phase: 260421-u72-git
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/lib.rs
  - src/lib/git.ts
autonomous: true
requirements:
  - u72-git
must_haves:
  truths:
    - "显示本地分支领先远端多少个提交"
    - "显示本地分支落后远端多少个提交"
    - "显示最后提交时间"
  artifacts:
    - path: "src-tauri/src/lib.rs"
      provides: "git status with ahead/behind/last_commit"
      min_lines: 25
    - path: "src/lib/git.ts"
      provides: "TypeScript interface and wrapper"
      min_lines: 15
  key_links:
    - from: "src-tauri/src/lib.rs::git_status"
      to: "src/lib/git.ts::getRemoteStatus"
      via: "invoke"
      pattern: "invoke.*git_status"
---

<objective>
根据git分支比较显示当前和远端领先多少个提交,落后多少个提交,最后提交是什么时间等详细信息

Purpose: 增强git状态显示,让用户清楚了解本地仓库与远端的同步状况
Output: 增强后的git_status命令,返回ahead/behind和lastCommitTime
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
当前git_status命令(见lib.rs 559-609行)只返回behind数量,需要增强为同时返回ahead和lastCommitTime
</context>

<tasks>

<task type="auto">
  <name>Task 1: 增强git_status返回ahead/behind和lastCommitTime</name>
  <files>src-tauri/src/lib.rs, src/lib/git.ts</files>
  <action>
1. 修改lib.rs中的git_status命令返回值,添加ahead和lastCommitTime字段:
   - 当前已计算behind,用graph_ahead_behind获取ahead和behind两个值
   - 使用repo.head()获取本地最新提交的Oid,然后用repo.find_commit(oid).time()获取提交时间

2. 修改src/lib/git.ts中的getRemoteStatus返回类型:
   - 添加ahead: number字段
   - 添加lastCommitTime: string字段

实现细节:
- 在lib.rs的git_status函数中,先用graph_ahead_behind获取(ahead, behind)元组
- 时间格式化为"%Y-%m-%d %H:%M:%S"字符串
- 返回JSON包含{ branch, hasUpdates, ahead, behind, lastCommitTime }
  </action>
  <verify>
    <automated>RUST_BACKEND能编译通过: cd src-tauri && cargo check 2>&1 | head -20</automated>
  </verify>
  <done>
git_status返回{ branch, hasUpdates, ahead, behind, lastCommitTime }五个字段
  </done>
</task>

</tasks>

<verification>
修改后的git_status命令能在Tauri后端编译通过
</verification>

<success_criteria>
返回ahead(领先提交数)、behind(落后提交数)、lastCommitTime(最后提交时间)
</success_criteria>

<output>
After completion, create `.planning/quick/260421-u72-git/260421-u72-SUMMARY.md`
</output>