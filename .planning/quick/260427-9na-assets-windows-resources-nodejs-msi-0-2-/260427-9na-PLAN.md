---
phase: quick
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/release.yml
  - src-tauri/src/lib.rs
  - package.json
  - CHANGELOG.md
  - src-tauri/assets-windows/node-v24.15.0-x64.msi (removed)
autonomous: true
must_haves:
  truths:
    - "Windows full 打包不再需要 assets-windows，MSI 直接从 src-tauri/resources/nodejs 获取"
    - "Release workflow 只校验 resources/nodejs 中的 MSI 存在，不再执行复制步骤"
  artifacts:
    - path: ".github/workflows/release.yml"
      contains: "resources/nodejs 直接作为 bundle 资产，校验步骤替代复制步骤"
    - path: "src-tauri/src/lib.rs"
      contains: "错误文案从 assets-windows 迁移到 resources/nodejs"
  key_links:
    - from: "resources/nodejs"
      to: "discover_windows_bundled_node_msi"
      via: "MSI 直接存放于 resources/nodejs，运行时与构建期使用同一目录"
---

# Quick Task 260427-9na Plan

## Goal

移除 `assets-windows` 目录，将 Node.js MSI 直接存放在 `src-tauri/resources/nodejs`，简化发布流程。

## Tasks

### Task 1
- files: `.github/workflows/release.yml`, `src-tauri/src/lib.rs`
- action: 删除 assets-windows 复制步骤，改为直接校验 resources/nodejs 中 MSI 存在；更新 Rust 错误文案和测试中的 assets-windows 引用。
- verify: `cargo test --manifest-path src-tauri/Cargo.toml --lib`
- done: 所有引用从 assets-windows 迁移到 resources/nodejs，测试通过。

### Task 2
- files: `package.json`, `CHANGELOG.md`
- action: Bump 版本到 2.0.13，添加 CHANGELOG 记录变更。
- done: 版本已更新，v0.2.13 标签已推送。
