# Plan: 260422-i24-update-progress

**Task:** 添加更新开始时的进度显示

**Description:** 点击更新按钮后立即显示进度，让更新过程感觉更平滑

## Tasks

### Task 1: 修改 handlePull 函数添加初始进度显示

**Files:**
- `src/app/page.tsx` (第 150-157 行)

**Action:**
在 handlePull 函数开始时设置初始进度：
1. 当按钮被点击时，立即设置 progress 为 `{ stage: 'pulling', percent: 5, label: '开始更新...' }`
2. 这样用户点击按钮后会立即看到进度条启动和 "开始更新..." 文字
3. 后端的进度事件会覆盖这个初始进度

**Verify:**
运行 `npx tsc --noEmit` 确保代码类型检查通过

**Done:**
- 在 handlePull 函数开始时设置初始进度
- 用户点击按钮后会立即看到进度条和 "开始更新..." 文字
- 后端进度事件会覆盖这个初始进度