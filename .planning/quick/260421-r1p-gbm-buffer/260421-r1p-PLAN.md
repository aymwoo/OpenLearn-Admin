---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/lib.rs
autonomous: true
must_haves:
  truths:
    - "GBM buffer error is identified and fixed"
  artifacts:
    - path: "src-tauri/src/lib.rs"
      contains: "buffer handling fix"
  key_links: []
---

<objective>
修复 GBM buffer 错误

Purpose: 识别并修复 Git 操作中的 buffer 相关错误
Output: 修复后的 lib.rs
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src-tauri/src/lib.rs
@src-tauri/Cargo.toml

# 已加载文件:
# - lib.rs: 739 行，包含 git2 所有操作
# - Cargo.toml: 包含 git2 = "0.20" 依赖
</context>

<tasks>

<task type="auto">
  <name>Task 1: 诊断 GBM buffer 错误</name>
  <files>src-tauri/src/lib.rs</files>
  <action>
检查 lib.rs 中所有 git2 buffer 使用场景:
1. 在 fetch_branch 函数中检查 fetch_options 和 remote_callbacks 的内存管理
2. 在 read_remote_file 中检查 blob.content() 调用是否正确处理
3. 在 copy_dir_recursive 中检查文件 buffer 复制逻辑
4. 特别检查 fs::read_dir 迭代器的 buffer 处理

常见 GBM buffer 错误位置:
- git2::buf::Buf 生命周期问题
- fs::read_dir 返回的 DirEntry buffer 泄漏
- blob.content() 的生命周期管理
- fetch 回调中的临时 buffer
  </action>
  <verify>
<automated>cargo check --manifest-path src-tauri/Cargo.toml 2>&1 | head -50</automated>
  </verify>
  <done>找到并修复 buffer 相关代码错误</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git2→memory | git2 库内部 buffer 管理 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-GBM-01 | D | git2 buffer | mitigate | 确保正确处理 git2::Buf 生命周期 |
| T-GBM-02 | R | fs buffer | mitigate | 使用正确的中转 buffer 大小 |
</threat_model>

<verification>
运行 cargo check 确保代码编译通过
</verification>

<success_criteria>
[ ] GBM buffer 错误已定位
[ ] 相关代码已修复
[ ] cargo check 通过
</success_criteria>

<output>
After completion, create `.planning/quick/260421-r1p-gbm-buffer/260421-r1p-SUMMARY.md`
</output>