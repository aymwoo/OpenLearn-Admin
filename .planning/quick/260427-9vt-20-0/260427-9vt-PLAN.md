---
phase: quick
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/GlobalProgress.tsx
  - src/app/page.tsx
autonomous: true
must_haves:
  truths:
    - "进度完成或失败时，用户能看到结果通知，不会瞬间消失"
    - "错误消息以可见方式展示，不再静默隐藏"
  artifacts:
    - path: "src/components/GlobalProgress.tsx"
      contains: "done/error 阶段延迟隐藏，展示结果消息"
    - path: "src/app/page.tsx"
      contains: "sync 结果通知 toast"
  key_links:
    - from: "pull-progress done/error event"
      to: "GlobalProgress + page notification"
      via: "完成或失败时展示结果消息，等待可见再隐藏"
---

# Quick Task 260427-9vt Plan

## Goal

修复更新进度闪结束的问题——进度到 20% 后闪回 0% 并消失，且无任何结果提示。

## Root Cause

1. `GlobalProgress.tsx:58`：`done`/`error`/`idle` 时直接 `return null`，结果瞬间消失
2. `page.tsx`：`message` 状态被设置但从未以 toast/通知形式渲染

## Tasks

### Task 1
- files: `src/components/GlobalProgress.tsx`
- action: 在 `done`/`error` 阶段不立即隐藏，改为显示结果消息（成功/失败），延迟 2-3 秒后再隐藏。成功显示绿色，错误显示红色。
- verify: eslint 通过，类型检查通过
- done: 进度条完成后用户能看到结果

### Task 2
- files: `src/app/page.tsx`
- action: 添加 sync 结果通知 toast，确保 message 有可见渲染。在 `done`/`error` 处理中显示通知。
- verify: eslint 通过，类型检查通过
- done: 用户能看到更新结果消息
